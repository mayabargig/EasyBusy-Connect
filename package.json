{
  "name": "easybusy-connect",
  "version": "1.0.0",
  "private": true,
  "description": "A social platform connecting local businesses and customers.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,magenta \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "lint": "npm run lint --workspace client",
    "check": "npm run check --workspace server && npm run lint --workspace client && npm run build --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
